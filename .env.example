# Database (Neon Postgres)
DATABASE_URL="postgresql://user:password@host:5432/hampstead_on_demand_v1"

# Auth.js / NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-me-with-random-secret"

# Email (nodemailer SMTP connection string)
EMAIL_SERVER="smtp://user:pass@smtp.example.com:587"
EMAIL_FROM="Hampstead On Demand <no-reply@yourdomain.com>"

# Vercel Blob (file uploads)
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# Dev only — bypasses auth for local testing (never set in production)
# AUTH_BYPASS="true"

# Optional: Sentry error monitoring
# SENTRY_DSN=""
# SENTRY_ORG=""
# SENTRY_PROJECT=""
# SENTRY_AUTH_TOKEN=""

# Optional: Google Places (postcode lookup)
# NEXT_PUBLIC_GOOGLE_PLACES_KEY=""
